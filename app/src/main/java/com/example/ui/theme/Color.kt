package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Medical Theme Colors
val MedicalTeal = Color(0xFF0D9488) // Sophisticated modern teal
val MedicalMint = Color(0xFF14B8A6) // Secondary mint
val MedicalGold = Color(0xFFF59E0B) // Warn near-expiry amber
val MedicalCrimson = Color(0xFFEF4444) // Danger expired red
val MedicalSlate = Color(0xFF0F172A) // Sleek slate text/primary dark
val MedicalLightBg = Color(0xFFF8FAFC) // Very light gray-blue background
val MedicalSurface = Color(0xFFFFFFFF) // Surface solid white

// Dark Theme fallback equivalents
val MedicalTealDark = Color(0xFF2DD4BF)
val MedicalMintDark = Color(0xFF34D399)
val MedicalSlateDark = Color(0xFF1E293B)
